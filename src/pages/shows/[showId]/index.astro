---
import Breadcrumbs from '@/components/Breadcrumbs.astro'
import EpisodeCard from '@/components/shows/EpisodeCard.astro'
import Layout from '@/layouts/Layout.astro'
import { getShows } from '@/lib/shows'
import { Icon } from 'astro-icon/components'

export async function getStaticPaths() {
  const shows = await getShows()
  return shows.map((show) => ({
    params: { showId: show.id },
    props: { show },
  }))
}

const { show } = Astro.props
---

<Layout title={show.title} description={show.description}>
  <main class="mx-auto max-w-(--breakpoint-xl) px-4 sm:px-6 lg:px-8">
    <div class="py-4">
      <Breadcrumbs
        items={[{ label: 'Shows', href: '/shows' }, { label: show.title }]}
      />
    </div>
    {/* Hero Section */}
    <div class="relative mt-4 h-[40vh] min-h-[400px] w-full">
      <div class="absolute inset-0">
        <img
          src={show.coverUrl}
          alt={show.title}
          class="h-full w-full object-cover opacity-20 blur-md"
        />
      </div>

      <div class="flex h-full flex-col justify-between">
        <div class="flex flex-col justify-center items-center text-center h-full space-y-1 p-8 z-10">
            <img
              src={show.logoUrl}
              alt={show.title}
              class="h-40 object-contain mb-6"
            />
          <h1 class="text-4xl font-bold tracking-tight md:text-5xl">
            {show.title}
          </h1>
          <p class="max-w-2xl text-lg text-muted-foreground">
            {show.description}
          </p>
          <div class="flex items-center gap-4 pt-4">
            <div class="flex items-center gap-2 text-sm text-muted-foreground">
              <Icon name="lucide:layers" class="size-4" />
              {show.seasons.length} Seasons
            </div>
          </div>
        </div>
      </div>
    </div>
    {/* Episodes Section */}
    <div class="container py-12">
      <div class="space-y-12">
        {
          show.seasons.map((season) => (
            <div class="space-y-6">
              <h2 class="border-b pb-2 text-2xl font-semibold tracking-tight">
                {season.title}
              </h2>
              <div class="grid gap-5 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">
                {season.episodes.map((episode) => (
                  <EpisodeCard
                    episode={episode}
                    href={`/shows/${show.id}/${season.id}/${episode.id}`}
                  />
                ))}
              </div>
            </div>
          ))
        }
      </div>
    </div>
  </main>
</Layout>
